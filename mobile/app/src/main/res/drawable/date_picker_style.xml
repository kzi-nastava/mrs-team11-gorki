<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Boja pozadine -->
    <solid android:color="@color/custom"/>

    <!-- Zaobljeni uglovi -->
    <corners android:radius="6dp"/>

    <!-- (Opcionalno) border/stroke -->
    <stroke
        android:width="2dp"
        android:color="@color/white"/>
</shape>